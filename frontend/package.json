{
  "name": "project-4_frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.3",
    "moment": "^2.22.1",
    "react": "^16.3.1",
    "react-big-calendar": "^0.19.0",
    "react-datetime": "^2.14.0",
    "react-dom": "^16.3.1",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "REACT_APP_API=http://localhost:4000 react-scripts start",
    "build": "REACT_APP_API=https://api-mapmypain.meichen.me react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "cross-env npm run build && now ./build -A ../now.json && now alias"
  },
  "devDependencies": {
    "cross-env": "^5.1.4"
  }
}
